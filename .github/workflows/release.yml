name: Build Release

on:
  release:
    types: [published]
  workflow_dispatch:  # Allow manual trigger

jobs:
  # Linux x86_64
  python-linux-x64:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: PyO3/maturin-action@v1
        with:
          working-directory: python
          target: x86_64
          args: --release --out dist -i python3.12
          manylinux: auto
      - uses: actions/upload-artifact@v4
        with:
          name: wheels-linux-x64
          path: python/dist/*.whl
      - uses: softprops/action-gh-release@v1
        if: github.event_name == 'release'
        with:
          files: python/dist/*.whl

  # Linux ARM64: Built manually on RPi5, not in CI
  # (GitHub's ARM64 runners require paid "larger runners")

  # macOS x86_64 (Intel) - cross-compiled from ARM64
  python-macos-x64:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: PyO3/maturin-action@v1
        with:
          working-directory: python
          target: x86_64-apple-darwin
          args: --release --out dist
      - uses: actions/upload-artifact@v4
        with:
          name: wheels-macos-x64
          path: python/dist/*.whl
      - uses: softprops/action-gh-release@v1
        if: github.event_name == 'release'
        with:
          files: python/dist/*.whl

  # macOS ARM64 (Apple Silicon)
  python-macos-arm64:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: PyO3/maturin-action@v1
        with:
          working-directory: python
          target: aarch64-apple-darwin
          args: --release --out dist
      - uses: actions/upload-artifact@v4
        with:
          name: wheels-macos-arm64
          path: python/dist/*.whl
      - uses: softprops/action-gh-release@v1
        if: github.event_name == 'release'
        with:
          files: python/dist/*.whl

  # Windows x86_64
  python-windows-x64:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: PyO3/maturin-action@v1
        with:
          working-directory: python
          target: x86_64
          args: --release --out dist
      - uses: actions/upload-artifact@v4
        with:
          name: wheels-windows-x64
          path: python/dist/*.whl
      - uses: softprops/action-gh-release@v1
        if: github.event_name == 'release'
        with:
          files: python/dist/*.whl

  # WASM package
  wasm:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: jetli/wasm-pack-action@v0.4.0
      - name: Build WASM (web target)
        run: cd wasm && wasm-pack build --target web
      - name: Package WASM
        run: cd wasm && zip -r ../praatfan-gpl.zip pkg/
      - uses: actions/upload-artifact@v4
        with:
          name: wasm-pkg
          path: praatfan-gpl.zip
      - uses: softprops/action-gh-release@v1
        if: github.event_name == 'release'
        with:
          files: praatfan-gpl.zip
